import ApplicationsTracker from "@/components/ApplicationsTracker";

export const dynamic = 'force-dynamic';

export default function DirectorTrackerPage() {
  return <ApplicationsTracker rolePath="director" />;
}